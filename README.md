# 🔍 FraudDetectAI - نظام كشف الاحتيال الذكي

<div dir="rtl">

## 📋 نظرة عامة
**FraudDetectAI** هو مشروع متكامل للكشف عن المعاملات المالية الاحتيالية باستخدام تقنيات **التعلم الآلي (Machine Learning)**. يستخدم المشروع بيانات محاكاة لمعاملات مالية ويطبق نماذج تعلم آلي متقدمة لتحديد الأنماط الاحتيالية.

### ✨ المميزات الرئيسية
- 🧹 **تنظيف وتحضير البيانات**: معالجة ملف CSV وتحويله إلى Parquet
- 📊 **تحليل استكشافي شامل (EDA)**: رسوم بيانية تفاعلية ومعلومات إحصائية
- ⚙️ **هندسة الخصائص**: بناء خصائص متقدمة لتحسين دقة النماذج
- 🤖 **3 نماذج أساسية**: Logistic Regression, Random Forest, XGBoost
- 📈 **قياس الأداء**: مقاييس ROC-AUC و PR-AUC
- ⚖️ **معالجة البيانات غير المتوازنة**: تقنيات متقدمة للتعامل مع Imbalanced Data

---

## 📂 هيكل المشروع المُحدث

```
FraudDetectAI_starter_v2/
│
├── README.md                         ← هذا الملف (الدليل الشامل)
├── requirements.txt                  ← المكتبات المطلوبة
│
├── scripts/                          ← سكريبتات الفحص والإعداد
│   ├── 1_setup_check.py              ← فحص وتثبيت المكتبات (بسيط)
│   └── 2_setup_check.py              ← نسخة محسّنة مع فحص تلقائي
│
├── src/                              ← الكود المصدري الرئيسي
│   ├── config.yaml                   ← إعدادات المسارات والتقسيم الزمني
│   │
│   ├── data/                         ← معالجة البيانات
│   │   └── load_data.py              ← تنظيف وتحضير البيانات (CSV → Parquet)
│   │
│   ├── eda/                          ← التحليل الاستكشافي
│   │   └── eda_starter.py            ← تحليل EDA ورسم الرسومات البيانية
│   │
│   ├── features/                     ← هندسة الخصائص
│   │   ├── build_features.py         ← تجهيز الخصائص (Feature Engineering)
│   │   └── feature_engineering_simple_gui.py ← واجهة Tkinter لـ Feature Engineering
│   │
│   └── models/                       ← النماذج
│       └── baseline.py               ← تدريب النماذج الأساسية (LogReg, RF, XGBoost)
│
├── data/                             ← مجلدات البيانات
│   ├── raw/                          ← البيانات الخام
│   │   ├── dataset_FraudDetectAI.csv ← ملف البيانات الأصلي
│   │   └── dataset_FraudDetectAI_1M.csv ← عينة مليون صف
│   │
│   └── processed/                    ← البيانات المعالجة
│       ├── paysim_clean_1M.parquet   ← البيانات المنظفة (Parquet)
│       └── paysim_features.parquet   ← البيانات مع الخصائص (Parquet)
│
├── models/                           ← النماذج المدربة والنتائج
│   └── baseline_metrics.csv          ← نتائج المقارنة بين النماذج
│
├── reports/                          ← التقارير والرسومات
│   └── figures/                      ← صور التحليل الاستكشافي
│       ├── hist_amount.png           ← توزيع المبالغ
│       ├── type_distribution.png     ← توزيع أنواع المعاملات
│       ├── fraud_rate_by_type.png    ← معدل الاحتيال حسب النوع
│       └── fraud_over_time.png       ← الاحتيال عبر الزمن
│
├── data_analyzer_gui.py              ← واجهة Tkinter لتحليل البيانات
└── data_analyzer_streamlit.py        ← واجهة Streamlit لتحليل البيانات
```

---

## 🚀 البدء السريع

### 1️⃣ متطلبات التشغيل
- **Python 3.7+**
- المكتبات في `requirements.txt`

### 2️⃣ تثبيت المكتبات

#### الطريقة الأولى (تلقائي):
```bash
python scripts/2_setup_check.py
```
سيفحص السكريبت جميع المكتبات ويثبّت المفقودة تلقائياً.

#### الطريقة الثانية (يدوي):
```bash
pip install -r requirements.txt
```

---

## 📝 خطوات التشغيل المُنفذة

### ✅ الخطوة 1: تنظيف البيانات 🧹
```bash
python src/data/load_data.py \
    --csv data/raw/dataset_FraudDetectAI.csv \
    --out data/processed/paysim_clean.parquet
```

**ما تم تنفيذه:**
- ✅ قراءة ملف CSV الأصلي (1,000,000 صف × 11 عمود)
- ✅ حذف الأعمدة غير الضرورية (`nameOrig`, `nameDest`)
- ✅ حساب خصائص جديدة (`deltaOrg`, `deltaDest`)
- ✅ حفظ البيانات بصيغة Parquet (أسرع وأصغر حجماً)
- ✅ إنشاء ملف CSV أيضاً للمراجعة

**النتائج:**
- `data/processed/paysim_clean_1M.parquet` - البيانات المنظفة (Parquet)
- `data/processed/paysim_clean_1M.csv` - البيانات المنظفة (CSV)

---

### ✅ الخطوة 2: هندسة الخصائص ⚙️
```bash
python src/features/build_features.py \
    --infile data/processed/paysim_clean_1M.parquet \
    --outfile data/processed/paysim_features.parquet
```

**ما تم تنفيذه:**
- ✅ إضافة **13 خصيصة جديدة** من البيانات الموجودة
- ✅ تحويل البيانات من 11 عمود إلى 24 عمود
- ✅ إنشاء ملفات Parquet و CSV

**الخصائص المُضافة (13 عمود):**

#### **🕐 الخصائص الزمنية (3 أعمدة):**
- `hour_of_day` - ساعة اليوم (0-23) من `step`
- `day_of_month` - يوم الشهر (0-29) من `step`
- `is_weekend` - هل هو عطلة نهاية الأسبوع؟ من `step`

#### **💰 خصائص المعاملات (3 أعمدة):**
- `amount_to_oldbalance_ratio` - نسبة المبلغ للرصيد الأصلي
- `account_emptied` - هل الحساب أصبح فارغاً؟ (مؤشر احتيال قوي)
- `dest_was_empty` - هل الحساب المستقبل كان فارغاً؟

#### **⚠️ خصائص الأخطاء (3 أعمدة):**
- `error_balance_orig` - خطأ في رصيد المرسل
- `error_balance_dest` - خطأ في رصيد المستقبل
- `has_balance_error` - هل يوجد خطأ في الحسابات؟ (98.5% من المعاملات!)

#### **🔢 تحويل الفئات (5 أعمدة):**
- `type_CASH_IN` - إيداع نقدي (21.9%)
- `type_CASH_OUT` - سحب نقدي (36.3%)
- `type_DEBIT` - خصم (0.6%)
- `type_PAYMENT` - دفع (33.0%)
- `type_TRANSFER` - تحويل (8.2%)

**النتائج:**
- `data/processed/paysim_features.parquet` - البيانات مع الخصائص (Parquet)

---

### ✅ الخطوة 3: واجهات تحليل البيانات 📊

#### **واجهة Tkinter (بسيطة):**
```bash
python data_analyzer_gui.py
```
- ✅ تحميل البيانات وعرضها
- ✅ مقارنة الأعمدة الأصلية مع الجديدة
- ✅ عرض إحصائيات مفصلة لكل خصيصة
- ✅ عرض عينة من البيانات

#### **واجهة Streamlit (متقدمة):**
```bash
streamlit run data_analyzer_streamlit.py
```
- ✅ واجهة ويب تفاعلية
- ✅ رسوم بيانية لتوزيع الخصائص
- ✅ جداول تفاعلية
- ✅ إحصائيات متقدمة

#### **واجهة Feature Engineering:**
```bash
python src/features/feature_engineering_simple_gui.py
```
- ✅ واجهة لتنفيذ Feature Engineering
- ✅ فحص البيانات قبل المعالجة
- ✅ بناء الخصائص بالضغط على زر
- ✅ عرض النتائج مباشرة

---

### ✅ الخطوة 4: تحليل النتائج 📈

**الإحصائيات المُكتشفة:**
- ✅ **98.5%** من المعاملات بها أخطاء في الحسابات
- ✅ **23.1%** من المعاملات تفرغ الحساب (مؤشر احتيال قوي)
- ✅ **41.1%** من المعاملات لحسابات فارغة
- ✅ **نسبة المبلغ للرصيد** في المتوسط 73.72% (عالية جداً!)

**الملفات المُنشأة:**
- ✅ `data_analyzer_gui.py` - واجهة Tkinter للتحليل
- ✅ `data_analyzer_streamlit.py` - واجهة Streamlit للتحليل
- ✅ `run_data_analyzer.py` - ملف التشغيل الموحد
- ✅ `simple_analyze.py` - تحليل سريع للبيانات

---

### 🔄 الخطوة 5: التحليل الاستكشافي (EDA) 📊
```bash
python src/eda/eda_starter.py \
    --infile data/processed/paysim_clean_1M.parquet
```

**الرسومات المُنتجة:**
- `hist_amount.png` - توزيع المبالغ المالية
- `type_distribution.png` - توزيع أنواع المعاملات
- `fraud_rate_by_type.png` - معدل الاحتيال حسب نوع المعاملة
- `fraud_over_time.png` - تطور الاحتيال عبر الزمن

جميع الرسومات تُحفظ في: `reports/figures/`

---

### 🔄 الخطوة 6: تدريب النماذج 🤖
```bash
python src/models/baseline.py \
    --infile data/processed/paysim_features.parquet \
    --modeldir models
```

**النماذج المُدربة:**
1. **Logistic Regression** - نموذج خطي بسيط
2. **Random Forest** - نموذج شجري متقدم
3. **XGBoost** - نموذج Gradient Boosting عالي الأداء

**النتائج:**
- ملف `models/baseline_metrics.csv` يحتوي على:
  - `model`: اسم النموذج
  - `roc_auc`: مقياس ROC-AUC
  - `pr_auc`: مقياس Precision-Recall AUC

---

## 🖥️ الواجهات التفاعلية المُنشأة

### **1️⃣ واجهة تحليل البيانات (Tkinter)**
```bash
python data_analyzer_gui.py
```

**المميزات:**
- ✅ **تحميل البيانات** - اختيار الملفات بسهولة
- ✅ **مقارنة الأعمدة** - رؤية الفرق بين البيانات المنظفة ومع الخصائص
- ✅ **عرض الإحصائيات** - تفاصيل دقيقة لكل خصيصة جديدة
- ✅ **عرض عينة البيانات** - رؤية البيانات الفعلية
- ✅ **واجهة بسيطة** - سهلة الاستخدام

### **2️⃣ واجهة تحليل البيانات (Streamlit)**
```bash
streamlit run data_analyzer_streamlit.py
```

**المميزات:**
- ✅ **واجهة ويب تفاعلية** - أكثر جمالاً وتفاعلاً
- ✅ **رسوم بيانية** - توزيع الخصائص بصرياً
- ✅ **جداول تفاعلية** - عرض البيانات بطريقة متقدمة
- ✅ **إحصائيات مفصلة** - مقاييس دقيقة مع رسوم بيانية
- ✅ **عرض متقدم** - أكثر تفصيلاً ووضوحاً

### **3️⃣ واجهة Feature Engineering**
```bash
python src/features/feature_engineering_simple_gui.py
```

**المميزات:**
- ✅ **فحص البيانات** - رؤية البيانات قبل المعالجة
- ✅ **بناء الخصائص** - تنفيذ Feature Engineering بالضغط على زر
- ✅ **عرض النتائج** - رؤية النتائج مباشرة
- ✅ **مراقبة التقدم** - شريط تقدم للعملية


---

## 📊 نتائج التحليل المُكتشفة

### **إحصائيات الخصائص الجديدة:**
- ✅ **98.5%** من المعاملات بها أخطاء في الحسابات (مؤشر احتيال قوي!)
- ✅ **23.1%** من المعاملات تفرغ الحساب (مؤشر احتيال قوي!)
- ✅ **41.1%** من المعاملات لحسابات فارغة
- ✅ **نسبة المبلغ للرصيد** في المتوسط 73.72% (عالية جداً!)

### **توزيع أنواع المعاملات:**
- ✅ **CASH_OUT**: 36.3% (أكثر أنواع الاحتيال)
- ✅ **PAYMENT**: 33.0% (ثاني أكثر الأنواع)
- ✅ **CASH_IN**: 21.9% (أقل احتيالاً)
- ✅ **TRANSFER**: 8.2% (احتيال متوسط)
- ✅ **DEBIT**: 0.6% (أقل الأنواع)

### **الخصائص الزمنية:**
- ✅ **ساعة اليوم**: المتوسط 14.86 ساعة (بعد الظهر)
- ✅ **يوم الشهر**: المتوسط 0.43 يوم (بداية الشهر)
- ✅ **عطلات نهاية الأسبوع**: 0% (لا توجد في البيانات)

---

## ⚙️ ملف الإعدادات `config.yaml`

```yaml
paths:
  raw_csv: data/raw/dataset_FraudDetectAI.csv      # مسار البيانات الخام
  cleaned: data/processed/paysim_clean.parquet     # مسار البيانات المنظفة
  figures: reports/figures                          # مسار الرسومات
  models_dir: models                                # مسار النماذج

training:
  test_hours_last: 96         # عدد الساعات الأخيرة للاختبار
  val_ratio: 0.15             # نسبة بيانات التحقق
  random_state: 42            # seed للتكرار
  n_jobs: -1                  # عدد المعالجات (-1 = جميع المعالجات)
```

يمكنك تعديل هذه الإعدادات حسب احتياجاتك.

---

## 📊 فهم البيانات

### الأعمدة الأساسية:
| العمود | الوصف |
|--------|-------|
| `step` | الخطوة الزمنية (1 خطوة = 1 ساعة) |
| `type` | نوع المعاملة (PAYMENT, TRANSFER, CASH_OUT, DEBIT, CASH_IN) |
| `amount` | المبلغ المالي |
| `oldbalanceOrg` | رصيد المرسل قبل المعاملة |
| `newbalanceOrig` | رصيد المرسل بعد المعاملة |
| `oldbalanceDest` | رصيد المستقبل قبل المعاملة |
| `newbalanceDest` | رصيد المستقبل بعد المعاملة |
| `isFraud` | هل المعاملة احتيالية؟ (0 = لا, 1 = نعم) |
| `isFlaggedFraud` | هل تم تعليم المعاملة كاحتيالية من النظام؟ |

### الخصائص المحسوبة:
- `deltaOrg` = `newbalanceOrig - (oldbalanceOrg - amount)`
- `deltaDest` = `newbalanceDest - (oldbalanceDest + amount)`

---

## 📈 تحليل نتائج البيانات المُنظفة

### توزيع أنواع المعاملات المالية

تُظهر البيانات المُنظفة توزيعاً واضحاً لأنواع المعاملات المالية، حيث يهيمن نوعان رئيسيان على السوق: **CASH_OUT (35.2%)** و **PAYMENT (33.8%)**، مما يشكل معاً ما يقارب **69%** من إجمالي المعاملات. يأتي بعدهما **CASH_IN (22.0%)** كأكثر أنواع الإيداع شيوعاً، بينما تُعتبر **TRANSFER (8.3%)** و **DEBIT (0.7%)** أقل الأنواع تكراراً. هذا التوزيع يُشير إلى طبيعة البيانات المحاكية التي تركز على المعاملات النقدية والمدفوعات اليومية.

### إحصائيات المبالغ المالية

تُظهر إحصائيات المبالغ توزيعاً غير طبيعي ومثيراً للاهتمام، حيث يتراوح المبلغ بين **$0.00** كحد أدنى و **$10,000,000.00** كحد أقصى بعد التنظيف. المتوسط الحسابي يبلغ **$172,362.50**، بينما الوسيط يقف عند **$74,784.77**، مما يُشير إلى وجود توزيع منحرف لليمين (Right-skewed distribution). هذا الفرق الكبير بين المتوسط والوسيط يُشير إلى وجود معاملات استثنائية كبيرة الحجم ترفع من قيمة المتوسط، مما قد يكون مؤشراً على معاملات مشبوهة أو احتيالية محتملة.

### مؤشرات الأمان والاحتيال

من منظور أمني، يُلاحظ أن الأنواع الأكثر خطورة للاحتيال - **CASH_OUT** و **TRANSFER** - تشكل معاً **43.5%** من إجمالي المعاملات، وهي نسبة عالية نسبياً. بالإضافة إلى ذلك، فإن وجود معاملات بمبالغ ضخمة (تصل إلى 10 ملايين دولار) مع متوسط مرتفع (172K دولار) يُشير إلى احتمالية وجود أنماط احتيالية متقدمة. هذا التوزيع غير المتوازن للمبالغ، مع التركيز على المعاملات النقدية والتحويلات، يُشكل بيئة مثالية لتطبيق نماذج التعلم الآلي للكشف عن الاحتيال، حيث يمكن للنماذج التعرف على الأنماط غير الطبيعية في هذه الأنواع من المعاملات.

---

## 📈 نتائج متوقعة

بعد تشغيل جميع الخطوات، ستحصل على:

### ✅ ملفات البيانات:
- ✔️ `data/processed/paysim_clean.parquet` - بيانات منظفة
- ✔️ `data/processed/paysim_features.parquet` - بيانات مع خصائص محسّنة

### ✅ الرسومات البيانية:
- ✔️ `reports/figures/hist_amount.png`
- ✔️ `reports/figures/type_distribution.png`
- ✔️ `reports/figures/fraud_rate_by_type.png`
- ✔️ `reports/figures/fraud_over_time.png`

### ✅ نتائج النماذج:
- ✔️ `models/baseline_metrics.csv`

**مثال على النتائج:**
```csv
model,roc_auc,pr_auc
LogReg,0.9234,0.6543
RF,0.9678,0.7821
XGB,0.9812,0.8234
```

---

## 🔧 استكشاف الأخطاء

### ❌ خطأ: `ModuleNotFoundError`
**الحل:**
```bash
python scripts/2_setup_check.py
```

### ❌ خطأ: `FileNotFoundError`
**الحل:** تأكد من وجود ملف البيانات في:
```
data/raw/dataset_FraudDetectAI.csv
```

### ❌ خطأ في الذاكرة (Memory Error)
**الحل:** استخدم عينة من البيانات:
```python
df = pd.read_csv('data/raw/dataset_FraudDetectAI.csv', nrows=100000)
```

---

## 🎯 الخطوات التالية (Advanced)

### 1. تحسين النماذج:
- تطبيق **Hyperparameter Tuning** (GridSearch / RandomSearch)
- استخدام **Cross-Validation**
- تجربة نماذج أخرى (LightGBM, CatBoost)

### 2. تقنيات متقدمة:
- **SMOTE** لموازنة البيانات
- **Feature Selection** لاختيار أفضل الخصائص
- **Ensemble Methods** لدمج النماذج

### 3. واجهة تفاعلية:
- بناء Dashboard باستخدام **Streamlit**
- إضافة **SHAP** لتفسير النماذج
- عرض النتائج بشكل تفاعلي

---

## 📁 الملفات المُنشأة حديثاً

### **الواجهات التفاعلية:**
- ✅ `data_analyzer_gui.py` - واجهة Tkinter لتحليل البيانات
- ✅ `data_analyzer_streamlit.py` - واجهة Streamlit لتحليل البيانات
- ✅ `src/features/feature_engineering_simple_gui.py` - واجهة Tkinter لـ Feature Engineering

### **البيانات المُعالجة:**
- ✅ `data/processed/paysim_clean_1M.parquet` - البيانات المنظفة (Parquet)
- ✅ `data/processed/paysim_features.parquet` - البيانات مع الخصائص (Parquet)

---

## 📚 المكتبات المستخدمة

| المكتبة | الوصف |
|---------|-------|
| `pandas` | معالجة البيانات |
| `numpy` | العمليات الرياضية |
| `scikit-learn` | نماذج التعلم الآلي |
| `xgboost` | XGBoost |
| `imbalanced-learn` | معالجة البيانات غير المتوازنة |
| `matplotlib` | الرسومات البيانية |
| `plotly` | رسومات تفاعلية |
| `streamlit` | واجهات ويب تفاعلية |
| `tkinter` | واجهات سطح المكتب |
| `pyyaml` | قراءة ملفات YAML |
| `shap` | تفسير النماذج |

---

## 👤 المؤلف
**Created for Najeeb**  Group Members Name
Norah Najr AlOtaibi 
Rouz Musaad Alanazi 
Aalyah Kareem 
Shahad aldurayi alharbi 
Mannar Refae

---

---
- بيانات **PaySim** المستخدمة في هذا المشروع
- مجتمع **scikit-learn** و **XGBoost**

---

<div align="center">

</div>

</div>
