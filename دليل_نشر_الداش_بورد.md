# دليل نشر الداش بورد على الإنترنت

هذا الدليل يشرح كيفية نشر الداش بورد على الإنترنت بحيث يمكن الوصول إليه عبر رابط من أي مكان.

---

## الطريقة 1: Streamlit Cloud (الأسهل والأسرع) ⭐ **موصى به**

### المميزات:
- ✅ مجاني تماماً
- ✅ سهل جداً (5 دقائق)
- ✅ رابط دائم
- ✅ تحديث تلقائي عند رفع الكود على GitHub
- ✅ لا يحتاج خادم خاص

### الخطوات:

#### 1. رفع المشروع على GitHub

**أ) إنشاء حساب GitHub:**
- اذهب إلى: https://github.com
- سجل حساب جديد (مجاني)

**ب) إنشاء Repository جديد:**
1. اضغط على زر `+` في الأعلى → `New repository`
2. اسم الـ Repository: `FraudDetectAI_Dashboard`
3. اختر `Public` (أو Private إذا أردت)
4. اضغط `Create repository`

**ج) رفع الملفات:**
```bash
# في مجلد المشروع
git init
git add .
git commit -m "Initial commit - Fraud Detection Dashboard"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/FraudDetectAI_Dashboard.git
git push -u origin main
```

**أو استخدم GitHub Desktop (أسهل):**
1. حمّل GitHub Desktop من: https://desktop.github.com
2. افتح GitHub Desktop
3. File → Add Local Repository
4. اختر مجلد المشروع
5. Commit & Push

#### 2. نشر على Streamlit Cloud

**أ) التسجيل:**
- اذهب إلى: https://streamlit.io/cloud
- اضغط `Sign up` وسجل بحساب GitHub

**ب) نشر التطبيق:**
1. اضغط `New app`
2. اختر الـ Repository: `FraudDetectAI_Dashboard`
3. Branch: `main`
4. Main file path: `dashboard/app.py`
5. اضغط `Deploy`

**ج) الحصول على الرابط:**
- بعد 2-3 دقائق، ستحصل على رابط مثل:
  ```
  https://your-app-name.streamlit.app
  ```
- هذا الرابط يمكن مشاركته مع المشرفة!

#### 3. إعدادات مهمة

**أ) ملف `.streamlit/config.toml` (اختياري):**
أنشئ ملف في جذر المشروع:
```
.streamlit/
  config.toml
```

محتوى الملف:
```toml
[server]
headless = true
port = 8501
enableCORS = false

[browser]
gatherUsageStats = false
```

**ب) ملف `requirements.txt` في جذر المشروع:**
تأكد أن الملف موجود ويحتوي على:
```
pandas
numpy
scikit-learn
xgboost
imbalanced-learn
matplotlib
plotly
streamlit
pyyaml
shap
pyarrow
```

**ج) ملف `.gitignore`:**
```
__pycache__/
*.pyc
*.pkl
*.csv
*.parquet
.env
.DS_Store
```

---

## الطريقة 2: ngrok (للاختبار السريع) 🚀

### المميزات:
- ✅ سريع جداً (دقيقة واحدة)
- ✅ جيد للعرض السريع
- ⚠️ الرابط يتغير في النسخة المجانية
- ⚠️ يحتاج جهازك يعمل

### الخطوات:

#### 1. تثبيت ngrok
- اذهب إلى: https://ngrok.com
- سجل حساب مجاني
- حمّل ngrok
- ثبت الملف في مجلد سهل الوصول

#### 2. تشغيل الداش بورد محلياً
```bash
cd dashboard
streamlit run app.py --server.port 8501
```

#### 3. تشغيل ngrok
```bash
ngrok http 8501
```

#### 4. الحصول على الرابط
- ستحصل على رابط مثل:
  ```
  https://abc123.ngrok.io
  ```
- شارك هذا الرابط مع المشرفة

**⚠️ ملاحظة:** 
- الرابط يعمل فقط طالما جهازك يعمل
- في النسخة المجانية، الرابط يتغير في كل مرة
- للرابط الدائم، تحتاج نسخة مدفوعة

---

## الطريقة 3: Heroku (خادم دائم)

### المميزات:
- ✅ رابط دائم
- ✅ يعمل 24/7
- ⚠️ يحتاج إعداد أكثر
- ⚠️ قد يحتاج بطاقة ائتمانية (لكن مجاني للاستخدام الأساسي)

### الخطوات:

#### 1. تثبيت Heroku CLI
- حمّل من: https://devcenter.heroku.com/articles/heroku-cli

#### 2. إنشاء ملفات الإعداد

**أ) `Procfile` (في جذر المشروع):**
```
web: cd dashboard && streamlit run app.py --server.port=$PORT --server.address=0.0.0.0
```

**ب) `setup.sh` (في جذر المشروع):**
```bash
mkdir -p ~/.streamlit/

echo "\
[server]\n\
headless = true\n\
port = $PORT\n\
enableCORS = false\n\
" > ~/.streamlit/config.toml
```

**ج) تعديل `requirements.txt`:**
تأكد أن الملف موجود ويحتوي على جميع المكتبات.

#### 3. نشر على Heroku
```bash
# تسجيل الدخول
heroku login

# إنشاء تطبيق
heroku create fraud-detect-dashboard

# رفع الملفات
git init
git add .
git commit -m "Deploy dashboard"
git push heroku main

# فتح التطبيق
heroku open
```

---

## الطريقة 4: VPS (خادم خاص)

### المميزات:
- ✅ تحكم كامل
- ✅ أداء عالي
- ⚠️ يحتاج معرفة تقنية
- ⚠️ تكلفة شهرية

### الخطوات:

#### 1. شراء VPS
- DigitalOcean: https://www.digitalocean.com
- AWS EC2: https://aws.amazon.com/ec2
- Azure: https://azure.microsoft.com

#### 2. إعداد الخادم
```bash
# تحديث النظام
sudo apt update && sudo apt upgrade -y

# تثبيت Python
sudo apt install python3 python3-pip -y

# تثبيت Streamlit
pip3 install streamlit

# رفع الملفات (استخدم SCP أو Git)
git clone YOUR_REPO_URL
cd FraudDetectAI_Dashboard

# تشغيل الداش بورد
streamlit run dashboard/app.py --server.port 8501 --server.address 0.0.0.0
```

#### 3. استخدام Nginx كـ Reverse Proxy
```bash
# تثبيت Nginx
sudo apt install nginx -y

# إعداد Nginx
sudo nano /etc/nginx/sites-available/streamlit
```

محتوى الملف:
```nginx
server {
    listen 80;
    server_name YOUR_DOMAIN_OR_IP;

    location / {
        proxy_pass http://localhost:8501;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

```bash
# تفعيل الموقع
sudo ln -s /etc/nginx/sites-available/streamlit /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

#### 4. استخدام PM2 لتشغيل دائم
```bash
# تثبيت PM2
npm install -g pm2

# تشغيل الداش بورد
pm2 start "streamlit run dashboard/app.py --server.port 8501 --server.address 0.0.0.0" --name fraud-dashboard

# حفظ الإعدادات
pm2 save
pm2 startup
```

---

## مقارنة الطرق

| الطريقة | السهولة | التكلفة | الرابط الدائم | الوقت |
|---------|---------|---------|---------------|-------|
| **Streamlit Cloud** | ⭐⭐⭐⭐⭐ | مجاني | ✅ | 5 دقائق |
| **ngrok** | ⭐⭐⭐⭐ | مجاني | ❌ | 1 دقيقة |
| **Heroku** | ⭐⭐⭐ | مجاني/مدفوع | ✅ | 15 دقيقة |
| **VPS** | ⭐⭐ | مدفوع | ✅ | 30+ دقيقة |

---

## التوصية النهائية

**للعرض السريع:** استخدم **ngrok** (دقيقة واحدة)

**للنشر الدائم:** استخدم **Streamlit Cloud** (5 دقائق، مجاني، رابط دائم)

---

## نصائح مهمة

### 1. تأكد من المسارات الصحيحة
في `dashboard/app.py`، تأكد أن المسارات صحيحة:
```python
BASE_DIR = Path(__file__).parent.parent
```

### 2. حجم البيانات
- Streamlit Cloud لديه حد 1GB للملفات
- إذا كانت البيانات كبيرة، استخدم Parquet بدلاً من CSV
- أو استخدم قاعدة بيانات خارجية

### 3. الأمان
- لا ترفع ملفات حساسة (API keys، passwords)
- استخدم `.gitignore` لحماية الملفات
- في Streamlit Cloud، يمكنك إضافة Secrets في Settings

### 4. الأداء
- استخدم `@st.cache_data` للبيانات الكبيرة
- استخدم Parquet بدلاً من CSV
- قلل حجم البيانات المعروضة

---

## حل المشاكل الشائعة

### المشكلة: "Module not found"
**الحل:** تأكد أن `requirements.txt` يحتوي على جميع المكتبات

### المشكلة: "File not found"
**الحل:** تحقق من المسارات في الكود، استخدم مسارات نسبية

### المشكلة: "Port already in use"
**الحل:** 
```bash
streamlit run app.py --server.port 8502
```

### المشكلة: "Memory error"
**الحل:** قلل حجم البيانات أو استخدم عينة أصغر

---

## مثال: إعداد سريع لـ Streamlit Cloud

### 1. إنشاء ملف `.streamlit/config.toml`:
```
.streamlit/
  config.toml
```

### 2. محتوى `config.toml`:
```toml
[server]
headless = true
port = 8501
enableCORS = false

[browser]
gatherUsageStats = false
```

### 3. رفع على GitHub:
```bash
git add .
git commit -m "Ready for deployment"
git push
```

### 4. نشر على Streamlit Cloud:
- اذهب إلى https://streamlit.io/cloud
- New app → اختر Repository → Deploy
- انتظر 2-3 دقائق
- احصل على الرابط!

---

## الخلاصة

**الطريقة الموصى بها: Streamlit Cloud**
- سهل جداً
- مجاني
- رابط دائم
- تحديث تلقائي

**الخطوات السريعة:**
1. رفع المشروع على GitHub
2. تسجيل في Streamlit Cloud
3. نشر التطبيق
4. مشاركة الرابط مع المشرفة

**الوقت الإجمالي: 5-10 دقائق** ⏱️

---

*تم إنشاء هذا الدليل لمساعدتك في نشر الداش بورد بسهولة*

