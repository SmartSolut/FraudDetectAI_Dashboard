# 🛠️ دليل تثبيت مشروع FraudDetectAI

## 📋 المتطلبات الأساسية

قبل البدء، تأكد من تثبيت:
- **Python 3.9+** (يفضل 3.10 أو 3.11)
- **pip** (مدير حزم Python)
- **Git** (اختياري - لتحميل المشروع)

---

## 🚀 خطوات التثبيت بالترتيب

### الخطوة 1: إنشاء بيئة افتراضية (مستحسن)

```bash
# إنشاء بيئة افتراضية
python -m venv venv

# تفعيل البيئة (Windows)
venv\Scripts\activate

# تفعيل البيئة (Mac/Linux)
source venv/bin/activate
```

---

### الخطوة 2: تثبيت المكتبات بالترتيب

#### 📦 المكتبات الأساسية (بالترتيب):

```bash
# 1. ترقية pip أولاً
pip install --upgrade pip

# 2. المكتبات الأساسية للبيانات
pip install numpy
pip install pandas
pip install pyarrow

# 3. مكتبات التعلم الآلي
pip install scikit-learn
pip install xgboost
pip install imbalanced-learn

# 4. مكتبات التفسير والتحليل
pip install shap

# 5. مكتبات الرسوم البيانية
pip install matplotlib
pip install plotly

# 6. مكتبة الداش بورد
pip install streamlit

# 7. مكتبات إضافية
pip install pyyaml
```

---

### الخطوة 3: التثبيت السريع (أمر واحد)

```bash
pip install numpy pandas pyarrow scikit-learn xgboost imbalanced-learn shap matplotlib plotly streamlit pyyaml
```

---

### الخطوة 4: التثبيت من ملف requirements.txt

```bash
pip install -r requirements.txt
```

---

## 📋 قائمة المكتبات الكاملة

| # | المكتبة | الوصف | الإصدار المقترح |
|---|---------|-------|-----------------|
| 1 | **numpy** | العمليات الرياضية والمصفوفات | >= 1.24.0 |
| 2 | **pandas** | معالجة وتحليل البيانات | >= 2.0.0 |
| 3 | **pyarrow** | قراءة ملفات Parquet | >= 14.0.0 |
| 4 | **scikit-learn** | نماذج التعلم الآلي | >= 1.3.0 |
| 5 | **xgboost** | نموذج XGBoost | >= 2.0.0 |
| 6 | **imbalanced-learn** | معالجة البيانات غير المتوازنة | >= 0.11.0 |
| 7 | **shap** | تفسير نتائج النماذج | >= 0.43.0 |
| 8 | **matplotlib** | الرسوم البيانية الأساسية | >= 3.7.0 |
| 9 | **plotly** | الرسوم البيانية التفاعلية | >= 5.18.0 |
| 10 | **streamlit** | بناء الداش بورد | >= 1.28.0 |
| 11 | **pyyaml** | قراءة ملفات الإعدادات | >= 6.0 |

---

## ✅ التحقق من التثبيت

```bash
# التحقق من جميع المكتبات
python -c "
import numpy; print(f'✅ numpy: {numpy.__version__}')
import pandas; print(f'✅ pandas: {pandas.__version__}')
import pyarrow; print(f'✅ pyarrow: {pyarrow.__version__}')
import sklearn; print(f'✅ scikit-learn: {sklearn.__version__}')
import xgboost; print(f'✅ xgboost: {xgboost.__version__}')
import imblearn; print(f'✅ imbalanced-learn: {imblearn.__version__}')
import shap; print(f'✅ shap: {shap.__version__}')
import matplotlib; print(f'✅ matplotlib: {matplotlib.__version__}')
import plotly; print(f'✅ plotly: {plotly.__version__}')
import streamlit; print(f'✅ streamlit: {streamlit.__version__}')
import yaml; print(f'✅ pyyaml: OK')
print('\\n🎉 جميع المكتبات مثبتة بنجاح!')
"
```

---

## 🚀 تشغيل الداش بورد

```bash
# من المجلد الرئيسي للمشروع
cd E:\python\FraudDetectAI_starter_v2
streamlit run dashboard/app.py
```

---

## ⚠️ حل المشاكل الشائعة

### مشكلة 1: خطأ في تثبيت XGBoost
```bash
# جرب هذا الأمر
pip install xgboost --no-cache-dir
```

### مشكلة 2: خطأ في تثبيت SHAP
```bash
# تأكد من تثبيت أدوات البناء أولاً (Windows)
pip install --upgrade setuptools wheel
pip install shap
```

### مشكلة 3: Streamlit لا يعمل
```bash
# أعد تثبيت Streamlit
pip uninstall streamlit
pip install streamlit --upgrade
```

### مشكلة 4: خطأ في قراءة ملفات Parquet
```bash
pip install pyarrow --upgrade
```

---

## 📁 هيكل المشروع المطلوب

```
FraudDetectAI_starter_v2/
├── dashboard/
│   ├── app.py              ← الداش بورد الرئيسي
│   └── utils.py            ← الدوال المساعدة
├── data/
│   └── processed/
│       └── test_featured.parquet  ← البيانات
├── models/
│   ├── random_forest_model.pkl    ← نموذج Random Forest
│   ├── xgboost_model.pkl          ← نموذج XGBoost
│   └── logistic_regression_model.pkl ← نموذج Logistic
├── results/
│   ├── model_metrics.csv          ← مقاييس الأداء
│   └── feature_importance_rf.csv  ← أهمية الخصائص
├── requirements.txt
└── README.md
```

---

## 🎯 ملخص الأوامر

```bash
# 1. إنشاء بيئة افتراضية
python -m venv venv
venv\Scripts\activate

# 2. تثبيت المكتبات
pip install -r requirements.txt

# 3. تشغيل الداش بورد
streamlit run dashboard/app.py
```

---

**🎉 مبروك! المشروع جاهز للعمل!**

