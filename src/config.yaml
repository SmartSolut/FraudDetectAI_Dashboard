paths:
  raw_csv: data/raw/dataset_FraudDetectAI.csv
  cleaned: data/processed/paysim_clean.parquet
  figures: reports/figures
  models_dir: models
training:
  test_hours_last: 96
  val_ratio: 0.15
  random_state: 42
  n_jobs: -1
